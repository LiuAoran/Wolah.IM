<UserControl x:Class="Wolah.IM.CustomControl.PasswordControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wolah.IM.CustomControl"
             xmlns:helper="clr-namespace:Wolah.IM.Helper"
             xmlns:converter="clr-namespace:Wolah.IM.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="36" d:DesignWidth="240">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resource/Style/TextBoxStyle.xaml"></ResourceDictionary>
                <ResourceDictionary Source="../Resource/Style/PasswordBoxStyle.xaml"></ResourceDictionary>
                <ResourceDictionary Source="../Resource/Style/CheckBoxStyle.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
            <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"></converter:BoolToVisibilityConverter>
            <converter:AntiBoolToVisibilityConverter x:Key="AntiBoolToVisibilityConverter"></converter:AntiBoolToVisibilityConverter>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="24"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <PasswordBox Grid.ColumnSpan="2" Style="{StaticResource RoundCornerPasswordBoxStyle}" helper:PasswordHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ElementName=DisplayPasswordChk, Path=IsChecked, Converter={StaticResource AntiBoolToVisibilityConverter}}"></PasswordBox>
        <TextBox Grid.ColumnSpan="2"  Style="{StaticResource PassowrdTextBoxStyle}" Visibility="{Binding ElementName=DisplayPasswordChk, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}" Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <TextBlock IsHitTestVisible="False" Text="{Binding Watermark, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center" FontSize="14" Margin="20,0,0,0" Foreground="#BBB"></TextBlock>
        <CheckBox x:Name="DisplayPasswordChk" Style="{StaticResource EyeCheckBoxStyle}" Height="16" Grid.Column="1" HorizontalAlignment="Left" Panel.ZIndex="1"></CheckBox>
    </Grid>
</UserControl>
